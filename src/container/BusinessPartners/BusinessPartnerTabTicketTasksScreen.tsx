import React from "react";
import { View, Text } from "react-native";

const BusinessPartnerTabTicketTasksScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>BusinessPartner Ticket Tasks Screen</Text>
      {/* Add your details screen UI and functionality */}
    </View>
  );
};

export default BusinessPartnerTabTicketTasksScreen;
