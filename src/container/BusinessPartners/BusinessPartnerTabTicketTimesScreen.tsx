import React from "react";
import { View, Text } from "react-native";

const BusinessPartnerTicketTimesScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>BusinessPartner Ticket Times Screen</Text>
      {/* Add your details screen UI and functionality */}
    </View>
  );
};

export default BusinessPartnerTicketTimesScreen;
